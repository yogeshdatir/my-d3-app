import React  from 'react';
import './App.css';

const App: React.FC = () => {

  return (
    <div>
      {/* default width of the svg is 300 */}
      <svg width={800} height={500}>
      </svg>
    </div>
  )
}

export default App;
